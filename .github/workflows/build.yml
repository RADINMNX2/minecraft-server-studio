name: Build Windows EXE

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust (stable, MSVC)
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Rust backend
        working-directory: backend
        run: cargo build --release

      - name: Stage backend binary
        run: |
          mkdir bin
          copy backend\target\release\mcss_backend.exe bin\mcss_backend.exe

      - name: Install npm dependencies
        run: npm install

      - name: Build Electron app
        run: npm run build

      - name: Build installer (.exe)
        run: npx electron-builder --win --x64 --publish never

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create ${{ github.ref_name }} release/MCSS-Setup-*.exe --title "Minecraft Server Studio ${{ github.ref_name }}" --generate-notes
